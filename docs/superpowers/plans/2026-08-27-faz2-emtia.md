# Faz 2: Emtia — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Katalogu çok-kaynaklı hale getirip Yahoo Finance'ten 10 emtia serisini iki yeni kategori sayfasında yayına almak.

**Architecture:** `Seri` bir `kaynak_tipi` alanı kazanır ve kaynağa özel alanlar (`evds_code`, `yahoo_symbol`) opsiyonel olur; `_dogrula` tipe göre doğrular. `ingest/yahoo.py`, `ingest/evds.py`'nin şeklini izleyen ikinci bir istemcidir. `ingest/run.py` kaynak tipine göre açık dallanır. Sayfa katmanı, yeni verinin görünmesi için değişmez.

**Tech Stack:** Python 3.12 · pandas · requests · PyYAML · pytest · Streamlit ≥1.49 · Plotly

**Spec:** `docs/superpowers/specs/2026-08-27-faz2-emtia-design.md`

## Global Constraints

- **Python 3.12.**
- **Arayüz dili Türkçe.** Tanımlayıcılar, fonksiyon adları, hata mesajları ve yorumlar Türkçe.
- **Veri dosyaları yalnızca `date,value` içerir.** Metadata katalogda yaşar.
- **CSV yazımı `float_format="%.5f"`.** Parquet'e geçilmez — ölçüldü, git'te %32 daha büyük.
- **Renk paleti `core/theme.py`'de kilitli.** Hiçbir hex değeri değiştirilmez.
- **Yahoo'ya yedek kaynak eklenmez.** Başarısızlık yüksek sesle olur: non-zero exit, kırmızı Actions, kısmi veri yayınlanmaz.
- **Hız sınırlama eklenmez.** 429 görülmedi; kanıtsız savunma kodu yazılmaz.
- **`EVDS_API_KEY` yalnızca `ingest/` tarafında okunur.** Streamlit kodunda geçmez.
- **Yahoo sembolleri kilitli:** `BZ=F`, `CL=F`, `NG=F`, `GC=F`, `SI=F`, `HG=F`, `HRC=F`, `PL=F`, `PA=F`, `ALI=F`. Hepsi 2026-08-27'de canlı doğrulandı.
- **Sayfa katmanı iddiası:** `core/components.py`, `core/charts.py`, `core/theme.py` ve `app.py` bu fazda hiç değişmez. (`core/page.py` yalnızca Task 4'te, kategori notu için tek satır alır.)

---

### Task 1: Katalog şemasını çok-kaynaklı hale getirme

`Seri` bir `kaynak_tipi` kazanır, kaynağa özel alanlar opsiyonel olur, doğrulama tipe göre dallanır. Mevcut 13 seri `kaynak_tipi: evds` alır. Bu görevin sonunda hiçbir davranış değişmemiş, yalnızca şema genişlemiştir.

**Files:**
- Modify: `core/catalog.py`
- Modify: `catalog/series.yaml` (13 seriye birer satır)
- Test: `tests/test_catalog.py`

**Interfaces:**
- Consumes: hiçbir şey (ilk görev)
- Produces:
  - `core.catalog.GECERLI_KAYNAK_TIPLERI: set[str]` — `{"evds", "yahoo"}`
  - `core.catalog.Seri` — alan sırası: `id, title, category, kaynak, kaynak_tipi, unit, freq, charts`, ardından varsayılanlı: `evds_code: str | None = None`, `evds_frequency: str | None = None`, `yahoo_symbol: str | None = None`, `monthly_agg: str = "mean"`, `start_date: str | None = None`
  - `core.catalog._dogrula(seri, kategori_sluglari, gorulen)` — kaynak tipine göre doğrular

- [ ] **Step 1: Testleri yaz (başarısız olmalı)**

`tests/test_catalog.py`'nin sonuna ekleyin. Dosyanın başında `dataclasses` ve `_dogrula` import'u gerekiyor; mevcut import bloğunu şu hâle getirin:

```python
import dataclasses

import pytest

from core.catalog import (
    KatalogHatasi,
    Seri,
    _dogrula,
    kategorileri_yukle,
    seri_getir,
    seri_listele,
    serileri_yukle,
)
```

Yeni testler:

```python
def _sluglar():
    return {k.slug for k in kategorileri_yukle()}


def test_her_serinin_kaynak_tipi_gecerli():
    for seri in serileri_yukle():
        assert seri.kaynak_tipi in {"evds", "yahoo"}, seri.id


def test_evds_serisi_sayisi_on_uc():
    # Göç kontrolü: 13 EVDS serisinin hepsi kaynak_tipi kazanmış olmalı.
    # Yahoo serileri Task 3'te ekleneceği için "hepsi evds" demiyoruz.
    assert sum(1 for s in serileri_yukle() if s.kaynak_tipi == "evds") == 13


def test_evds_serisinde_evds_code_zorunlu():
    seri = dataclasses.replace(seri_getir("enflasyon/tufe-genel"), evds_code=None)
    with pytest.raises(KatalogHatasi, match="evds_code"):
        _dogrula(seri, _sluglar(), set())


def test_evds_serisinde_gecersiz_frekans_reddedilir():
    seri = dataclasses.replace(seri_getir("enflasyon/tufe-genel"), evds_frequency="9")
    with pytest.raises(KatalogHatasi, match="evds_frequency"):
        _dogrula(seri, _sluglar(), set())


def test_yahoo_serisinde_yahoo_symbol_zorunlu():
    seri = dataclasses.replace(
        seri_getir("enflasyon/tufe-genel"),
        kaynak_tipi="yahoo",
        yahoo_symbol=None,
    )
    with pytest.raises(KatalogHatasi, match="yahoo_symbol"):
        _dogrula(seri, _sluglar(), set())


def test_yahoo_serisi_evds_code_gerektirmez():
    seri = dataclasses.replace(
        seri_getir("enflasyon/tufe-genel"),
        kaynak_tipi="yahoo",
        yahoo_symbol="BZ=F",
        evds_code=None,
        evds_frequency=None,
    )
    _dogrula(seri, _sluglar(), set())  # hata atmamalı


def test_bilinmeyen_kaynak_tipi_reddedilir():
    seri = dataclasses.replace(seri_getir("enflasyon/tufe-genel"), kaynak_tipi="bloomberg")
    with pytest.raises(KatalogHatasi, match="kaynak_tipi"):
        _dogrula(seri, _sluglar(), set())
```

- [ ] **Step 2: Mevcut testi kaynak-tipine duyarlı hale getir**

`test_alan_degerleri_gecerli_kumelerde` şu an *tüm* seriler için `evds_frequency` kontrol ediyor. Task 3'te Yahoo serileri eklendiğinde bu test kırılır. Şimdi düzeltin — gövdesini şununla değiştirin:

```python
def test_alan_degerleri_gecerli_kumelerde():
    for seri in serileri_yukle():
        assert seri.freq in {"daily", "weekly", "monthly"}, seri.id
        assert seri.monthly_agg in {"mean", "last", "sum"}, seri.id
        assert seri.charts, seri.id
        assert set(seri.charts) <= {"seasonality", "level"}, seri.id
        if seri.kaynak_tipi == "evds":
            assert seri.evds_frequency in {"1", "2", "5"}, seri.id
            assert seri.evds_code, seri.id
        if seri.kaynak_tipi == "yahoo":
            assert seri.yahoo_symbol, seri.id
```

Ayrıca `test_seri_alanlari_dogru_tiplerde` içine bir satır ekleyin:

```python
    assert seri.kaynak_tipi == "evds"
```

- [ ] **Step 3: Testleri koştur, başarısız olduklarını gör**

Run: `.venv/bin/python -m pytest tests/test_catalog.py -v`
Expected: FAIL — yeni testlerin çoğu `AttributeError: 'Seri' object has no attribute 'kaynak_tipi'`, `dataclasses.replace(..., kaynak_tipi=...)` çağıranlar ise `TypeError: ... got an unexpected keyword argument 'kaynak_tipi'`. (`_dogrula` zaten modülde var, sadece import edilmemişti — o yüzden ImportError beklemeyin.)

- [ ] **Step 4: `core/catalog.py`'yi güncelle**

Sabitlere ekleyin (mevcut `GECERLI_AYLIK_AGG` satırının altına):

```python
GECERLI_KAYNAK_TIPLERI = {"evds", "yahoo"}
```

`Seri` dataclass'ını şununla değiştirin:

```python
@dataclass(frozen=True)
class Seri:
    id: str
    title: str
    category: str
    kaynak: Kaynak
    kaynak_tipi: str
    unit: str
    freq: str
    charts: tuple[str, ...]
    evds_code: str | None = None
    evds_frequency: str | None = None
    yahoo_symbol: str | None = None
    monthly_agg: str = "mean"
    start_date: str | None = None
```

`serileri_yukle` içindeki `Seri(...)` çağrısını şununla değiştirin:

```python
        evds_frekans = ham.get("evds_frequency")
        seri = Seri(
            id=ham["id"],
            title=ham["title"],
            category=ham["category"],
            kaynak=Kaynak(**ham["kaynak"]),
            kaynak_tipi=ham["kaynak_tipi"],
            unit=ham["unit"],
            freq=ham["freq"],
            charts=tuple(ham["charts"]),
            evds_code=ham.get("evds_code"),
            evds_frequency=None if evds_frekans is None else str(evds_frekans),
            yahoo_symbol=ham.get("yahoo_symbol"),
            monthly_agg=ham.get("monthly_agg", "mean"),
            start_date=ham.get("start_date"),
        )
```

`_dogrula` içindeki iki blok değişir. Şu iki kontrolü **silin**:

```python
    if seri.evds_frequency not in GECERLI_EVDS_FREKANSLARI:
        raise KatalogHatasi(
            f"{seri.id}: geçersiz evds_frequency '{seri.evds_frequency}'"
        )
```

ve

```python
    if not seri.evds_code:
        raise KatalogHatasi(f"{seri.id}: evds_code boş")
```

Yerlerine, fonksiyonun sonuna şunu ekleyin:

```python
    if seri.kaynak_tipi not in GECERLI_KAYNAK_TIPLERI:
        raise KatalogHatasi(f"{seri.id}: geçersiz kaynak_tipi '{seri.kaynak_tipi}'")
    if seri.kaynak_tipi == "evds":
        if not seri.evds_code:
            raise KatalogHatasi(f"{seri.id}: evds kaynağı için evds_code zorunlu")
        if seri.evds_frequency not in GECERLI_EVDS_FREKANSLARI:
            raise KatalogHatasi(
                f"{seri.id}: geçersiz evds_frequency '{seri.evds_frequency}'"
            )
    if seri.kaynak_tipi == "yahoo":
        if not seri.yahoo_symbol:
            raise KatalogHatasi(f"{seri.id}: yahoo kaynağı için yahoo_symbol zorunlu")
```

- [ ] **Step 5: 13 mevcut seriye `kaynak_tipi: evds` ekle**

`catalog/series.yaml`'daki her kaydın `kaynak:` satırının hemen altına ekleyin:

```yaml
  kaynak_tipi: evds
```

13 kaydın hepsinde olmalı. Kontrol:

```bash
grep -c "kaynak_tipi: evds" catalog/series.yaml   # 13 vermeli
```

- [ ] **Step 6: Testleri koştur, geçtiklerini gör**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS — 66 mevcut + 7 yeni = 73 test. Sayı farklı çıkarsa testleri sayıya uydurmayın, bildirin.

- [ ] **Step 7: Commit**

```bash
git add core/catalog.py catalog/series.yaml tests/test_catalog.py
git commit -m "feat: katalog şeması çok-kaynaklı

Seri bir kaynak_tipi kazandı; evds_code ve yahoo_symbol opsiyonel oldu.
_dogrula kaynak tipine göre dallanıyor. Davranış değişmedi."
```

---

### Task 2: Yahoo Finance ingest modülü

`ingest/yahoo.py` — saf parse fonksiyonları ve `seri_cek()`. API key gerektirmez. Bu görevde ağa çıkılmaz; tüm testler fixture ile çalışır.

**Files:**
- Create: `ingest/yahoo.py`
- Test: `tests/test_yahoo.py`

**Interfaces:**
- Consumes: `core.catalog.Seri` (alan: `yahoo_symbol`)
- Produces:
  - `ingest.yahoo.ENDPOINT: str`
  - `ingest.yahoo.KULLANICI_AJANI: str`
  - `ingest.yahoo.sembol_kodla(sembol: str) -> str`
  - `ingest.yahoo.gun_yuvarla(ts: int) -> str` — ISO `YYYY-MM-DD`
  - `ingest.yahoo.noktalari_ayikla(yanit: dict) -> list[tuple[str, float]]`
  - `ingest.yahoo.seri_cek(seri: Seri, session=None) -> pd.DataFrame` — sütunlar `date, value`

- [ ] **Step 1: Testleri yaz (başarısız olmalı)**

`tests/test_yahoo.py`:

```python
from datetime import datetime, timezone

from ingest.yahoo import gun_yuvarla, noktalari_ayikla, sembol_kodla


def ts(yil, ay, gun, saat):
    return int(datetime(yil, ay, gun, saat, tzinfo=timezone.utc).timestamp())


def test_sembol_kodla_esittir_isaretini_kodlar():
    assert sembol_kodla("BZ=F") == "BZ%3DF"


def test_sembol_kodla_sade_sembole_dokunmaz():
    assert sembol_kodla("AAPL") == "AAPL"


def test_gun_yuvarla_yaz_saatinde_dogru_gunu_verir():
    # NY Mercantile yaz saati: borsa yerel gece yarısı 04:00 UTC
    assert gun_yuvarla(ts(2026, 8, 27, 4)) == "2026-08-27"


def test_gun_yuvarla_kis_saatinde_dogru_gunu_verir():
    # kış saati: borsa yerel gece yarısı 05:00 UTC
    assert gun_yuvarla(ts(2026, 1, 15, 5)) == "2026-01-15"


def test_gun_yuvarla_utc_onundeki_borsayi_ileri_yuvarlar():
    # UTC+2 borsası: yerel gece yarısı bir önceki günün 22:00 UTC'sidir
    assert gun_yuvarla(ts(2026, 3, 10, 22)) == "2026-03-11"


def test_noktalari_ayikla_nulllari_atar():
    yanit = {
        "chart": {
            "result": [
                {
                    "timestamp": [ts(2026, 1, 5, 5), ts(2026, 1, 6, 5), ts(2026, 1, 7, 5)],
                    "indicators": {"quote": [{"close": [80.5, None, 82.25]}]},
                }
            ]
        }
    }
    assert noktalari_ayikla(yanit) == [("2026-01-05", 80.5), ("2026-01-07", 82.25)]


def test_noktalari_ayikla_ardisik_ayni_tarihi_tekillestirir():
    yanit = {
        "chart": {
            "result": [
                {
                    "timestamp": [ts(2026, 1, 5, 4), ts(2026, 1, 5, 6)],
                    "indicators": {"quote": [{"close": [80.5, 81.0]}]},
                }
            ]
        }
    }
    assert noktalari_ayikla(yanit) == [("2026-01-05", 80.5)]


def test_noktalari_ayikla_bos_yanitta_bos_liste():
    assert noktalari_ayikla({}) == []
    assert noktalari_ayikla({"chart": {"result": []}}) == []
```

- [ ] **Step 2: Testleri koştur, başarısız olduklarını gör**

Run: `.venv/bin/python -m pytest tests/test_yahoo.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'ingest.yahoo'`

- [ ] **Step 3: `ingest/yahoo.py`'yi yaz**

```python
"""Yahoo Finance chart API istemcisi.

Dokümante edilmemiş davranışlar. Bunlar Faz 0'daki TypeScript modülünde
keşfedilip 2026-08-27'de yeniden doğrulanmıştır — sıfırdan keşfetmeye
çalışmayın:

- Yahoo tarayıcı User-Agent'larını 429 ile reddeder; Googlebot UA'sı
  kabul edilir.
- Semboldeki "=" URL'de kodlanmalıdır: BZ=F -> BZ%3DF
- Günlük barlar borsa yerel gece yarısına damgalanır. Timestamp'i en yakın
  UTC gece yarısına yuvarlamak DST'nin her iki yönünde de borsa yerel
  takvim gününü verir; meta.gmtoffset'e bağlanmak mevsimsel kayma üretir.
- close[] dizisi tatil ve işlem durması günlerinde null içerir.

Yedek kaynak yoktur (bilinçli karar): başarısızlık yüksek sesle olur.
"""

from __future__ import annotations

import math
from datetime import datetime, timezone
from urllib.parse import quote

import pandas as pd
import requests

from core.catalog import Seri

ENDPOINT = "https://query1.finance.yahoo.com/v8/finance/chart"
ARALIK = "15y"
ZAMAN_ASIMI = 60
KULLANICI_AJANI = (
    "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)"
)


def sembol_kodla(sembol: str) -> str:
    return quote(sembol, safe="")


def gun_yuvarla(ts: int) -> str:
    """Borsa yerel gece yarısına damgalı timestamp'i takvim gününe çevirir."""
    gun = round(ts / 86400) * 86400
    return datetime.fromtimestamp(gun, tz=timezone.utc).date().isoformat()


def noktalari_ayikla(yanit: dict) -> list[tuple[str, float]]:
    sonuclar = (yanit.get("chart") or {}).get("result") or []
    if not sonuclar:
        return []

    ilk = sonuclar[0]
    zamanlar = ilk.get("timestamp") or []
    kotalar = (ilk.get("indicators") or {}).get("quote") or [{}]
    kapanislar = kotalar[0].get("close") or []

    noktalar: list[tuple[str, float]] = []
    onceki: str | None = None
    for zaman, kapanis in zip(zamanlar, kapanislar):
        if kapanis is None or not math.isfinite(kapanis):
            continue
        tarih = gun_yuvarla(zaman)
        if tarih == onceki:
            continue
        onceki = tarih
        noktalar.append((tarih, float(kapanis)))
    return noktalar


def seri_cek(seri: Seri, session: requests.Session | None = None) -> pd.DataFrame:
    """Sembolün son 15 yıllık günlük kapanışlarını çeker."""
    http = session or requests
    url = (
        f"{ENDPOINT}/{sembol_kodla(seri.yahoo_symbol)}"
        f"?range={ARALIK}&interval=1d"
    )
    yanit = http.get(
        url,
        headers={"User-Agent": KULLANICI_AJANI, "Accept": "application/json"},
        timeout=ZAMAN_ASIMI,
    )
    if yanit.status_code != 200:
        raise RuntimeError(f"Yahoo HTTP {yanit.status_code} ({seri.yahoo_symbol})")

    noktalar = noktalari_ayikla(yanit.json())
    if not noktalar:
        raise RuntimeError(f"Yahoo boş seri döndürdü ({seri.yahoo_symbol})")

    return pd.DataFrame(noktalar, columns=["date", "value"])
```

- [ ] **Step 4: Testleri koştur, geçtiklerini gör**

Run: `.venv/bin/python -m pytest tests/test_yahoo.py -v`
Expected: PASS — 8 test. Tüm paket: 81.

- [ ] **Step 5: Commit**

```bash
git add ingest/yahoo.py tests/test_yahoo.py
git commit -m "feat: Yahoo Finance ingest modülü

UA tuzağı ve DST'ye dayanıklı tarih yuvarlama Faz 0'daki TS modülünden
taşındı ve yeniden doğrulandı. Yedek kaynak yok."
```

---

### Task 3: Orchestrator dallanması, katalog girdileri ve ilk gerçek çekim

`run.py` kaynak tipine göre dallanır, `EVDS_API_KEY` kontrolü koşullu olur, iki kategori ve on seri katalogda tanımlanır, gerçek veri çekilip commit'lenir.

**Files:**
- Modify: `ingest/run.py`
- Modify: `catalog/categories.yaml`
- Modify: `catalog/series.yaml`
- Test: `tests/test_run.py` (yeni)
- Generate: `data/emtia-enerji/*.csv`, `data/emtia-metaller/*.csv`

**Interfaces:**
- Consumes: `core.catalog.Seri`, `core.catalog.seri_listele`, `core.catalog.seri_getir`, `core.data.seri_yolu`, `ingest.evds.seri_cek(seri, api_key, session=None, bugun=None)`, `ingest.yahoo.seri_cek(seri, session=None)`
- Produces: `ingest.run._cek(seri, api_key, oturum) -> pd.DataFrame`

- [ ] **Step 1: Kategorileri ekle**

`catalog/categories.yaml` — `ekonomi-makro` kaydının hemen altına ekleyin (günlük piyasa verisi üstte toplansın):

```yaml
- slug: emtia-enerji
  title: Emtia · Enerji
- slug: emtia-metaller
  title: Emtia · Metaller
```

- [ ] **Step 2: On seriyi katalogda tanımla**

`catalog/series.yaml`'ın sonuna ekleyin. **Sıra bilinçlidir:** KPI satırı kategorinin ilk dört serisini gösterir, bu yüzden Metaller'de Altın/Gümüş/Bakır/HRC Çelik önde.

```yaml
- id: emtia-enerji/brent
  title: Brent Petrol
  category: emtia-enerji
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/BZ=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "BZ=F"
  unit: "USD/varil"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-enerji/wti
  title: WTI Ham Petrol
  category: emtia-enerji
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/CL=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "CL=F"
  unit: "USD/varil"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-enerji/dogalgaz
  title: Doğal Gaz (Henry Hub)
  category: emtia-enerji
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/NG=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "NG=F"
  unit: "USD/MMBtu"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/altin
  title: Altın
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/GC=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "GC=F"
  unit: "USD/ons"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/gumus
  title: Gümüş
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/SI=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "SI=F"
  unit: "USD/ons"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/bakir
  title: Bakır
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/HG=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "HG=F"
  unit: "USD/libre"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/hrc-celik
  title: HRC Çelik
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/HRC=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "HRC=F"
  unit: "USD/ton"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/platin
  title: Platin
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/PL=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "PL=F"
  unit: "USD/ons"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/paladyum
  title: Paladyum
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/PA=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "PA=F"
  unit: "USD/ons"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]

- id: emtia-metaller/aluminyum
  title: Alüminyum
  category: emtia-metaller
  kaynak: { name: Yahoo Finance, url: "https://finance.yahoo.com/quote/ALI=F" }
  kaynak_tipi: yahoo
  yahoo_symbol: "ALI=F"
  unit: "USD/ton"
  freq: daily
  monthly_agg: mean
  charts: [level, seasonality]
```

- [ ] **Step 3: Dispatch testlerini yaz (başarısız olmalı)**

`tests/test_run.py`:

```python
import dataclasses

import pandas as pd
import pytest

from core.catalog import seri_getir
from ingest.run import _cek


def sahte_df():
    return pd.DataFrame({"date": ["2026-01-01"], "value": [1.0]})


def test_cek_yahoo_serisini_yahoo_moduline_yonlendirir(monkeypatch):
    gorulen = {}

    def sahte(seri, session=None):
        gorulen["id"] = seri.id
        return sahte_df()

    monkeypatch.setattr("ingest.run.yahoo.seri_cek", sahte)
    _cek(seri_getir("emtia-enerji/brent"), None, None)
    assert gorulen["id"] == "emtia-enerji/brent"


def test_cek_evds_serisini_evds_moduline_yonlendirir(monkeypatch):
    gorulen = {}

    def sahte(seri, api_key, session=None, bugun=None):
        gorulen["id"] = seri.id
        gorulen["key"] = api_key
        return sahte_df()

    monkeypatch.setattr("ingest.run.evds.seri_cek", sahte)
    _cek(seri_getir("enflasyon/tufe-genel"), "gizli", None)
    assert gorulen["id"] == "enflasyon/tufe-genel"
    assert gorulen["key"] == "gizli"


def test_cek_bilinmeyen_kaynak_tipinde_hata():
    seri = dataclasses.replace(seri_getir("enflasyon/tufe-genel"), kaynak_tipi="bloomberg")
    with pytest.raises(ValueError, match="Bilinmeyen kaynak tipi"):
        _cek(seri, "gizli", None)
```

- [ ] **Step 4: Testleri koştur, başarısız olduklarını gör**

Run: `.venv/bin/python -m pytest tests/test_run.py -v`
Expected: FAIL — `ImportError: cannot import name '_cek' from 'ingest.run'`

- [ ] **Step 5: `ingest/run.py`'yi güncelle**

Import bloğunu şununla değiştirin:

```python
from core.catalog import Seri, seri_listele
from core.data import seri_yolu
from ingest import evds, yahoo
```

`seriyi_yaz`'ın hemen üstüne ekleyin:

```python
def _cek(seri: Seri, api_key: str | None, oturum):
    """Seriyi kaynak tipine göre doğru istemciye yönlendirir."""
    if seri.kaynak_tipi == "evds":
        return evds.seri_cek(seri, api_key, session=oturum)
    if seri.kaynak_tipi == "yahoo":
        return yahoo.seri_cek(seri, session=oturum)
    raise ValueError(f"Bilinmeyen kaynak tipi: {seri.kaynak_tipi}")
```

`main()` içinde argparse açıklamasını ve key kontrolünü değiştirin. Şu blok:

```python
    ayristirici = argparse.ArgumentParser(description="EVDS verilerini çeker")
    ayristirici.add_argument(
        "--only", help="Yalnızca bu seri id'sini çek (hata ayıklama için)"
    )
    args = ayristirici.parse_args()

    api_key = os.environ.get("EVDS_API_KEY")
    if not api_key:
        print("HATA: EVDS_API_KEY tanımlı değil", file=sys.stderr)
        return 2

    seriler = seri_listele()
    if args.only:
        seriler = [s for s in seriler if s.id == args.only]
        if not seriler:
            print(f"HATA: katalogda yok: {args.only}", file=sys.stderr)
            return 2
```

şu hâle gelir:

```python
    ayristirici = argparse.ArgumentParser(
        description="Katalogdaki serileri kaynaklarından çeker"
    )
    ayristirici.add_argument(
        "--only", help="Yalnızca bu seri id'sini çek (hata ayıklama için)"
    )
    args = ayristirici.parse_args()

    seriler = seri_listele()
    if args.only:
        seriler = [s for s in seriler if s.id == args.only]
        if not seriler:
            print(f"HATA: katalogda yok: {args.only}", file=sys.stderr)
            return 2

    api_key = os.environ.get("EVDS_API_KEY")
    if any(s.kaynak_tipi == "evds" for s in seriler) and not api_key:
        print("HATA: EVDS_API_KEY tanımlı değil veya boş", file=sys.stderr)
        return 2
```

Sıra değişti: seri listesi artık key kontrolünden önce belirleniyor, böylece yalnızca Yahoo serisi çeken bir koşu key istemiyor. Mesaj da "veya boş" kazandı — boş secret bir kez hata ayıklama turuna mal olmuştu.

Son olarak döngüdeki çağrıyı değiştirin:

```python
                df = _cek(seri, api_key, oturum)
```

- [ ] **Step 6: Testleri koştur, geçtiklerini gör**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS — 84 test (81 + 3 yeni).

- [ ] **Step 7: Key'siz Yahoo çekimini doğrula**

```bash
env -u EVDS_API_KEY .venv/bin/python -m ingest.run --only emtia-enerji/brent
```

Expected: `✓ emtia-enerji/brent — ~3770 nokta` ve `1/1 seri başarılı`. Key olmadan çalışması koşullu kontrolün kanıtıdır.

- [ ] **Step 8: Tam çekimi koştur**

```bash
EVDS_API_KEY=<key> .venv/bin/python -m ingest.run
```

Expected: `23/23 seri başarılı`.

Key'i siz sağlayamıyorsanız durun ve bildirin — bu adım gerçek bir kimlik bilgisi gerektirir.

- [ ] **Step 9: Üretilen veriyi gözle doğrula**

```bash
for f in data/emtia-*/*.csv; do echo "$f: $(tail -1 $f)"; done
wc -l data/emtia-*/*.csv | tail -1
```

Beklenen büyüklük mertebeleri (2026-08 itibarıyla): Brent ve WTI 80–90 USD, Altın ~4650 USD, Gümüş ~69 USD, Bakır ~6,7 USD, HRC Çelik ~1190 USD, Platin ~1830 USD, Paladyum ~1330 USD, Alüminyum ~3360 USD, Doğal Gaz ~2,9 USD. Bir seri bu mertebenin dışındaysa sembol yanlış eşlenmiş olabilir — durun ve bildirin.

- [ ] **Step 10: Commit**

```bash
git add ingest/run.py catalog/ tests/test_run.py data/
git commit -m "feat: emtia serileri ve kaynak tipine göre dallanma

run.py kaynak tipine göre yönlendiriyor; EVDS_API_KEY kontrolü artık
koşuya gerçekten EVDS serisi giriyorsa yapılıyor. 10 emtia serisi
katalogda, ilk gerçek çekim yapıldı."
```

---

### Task 4: Kategori notu ve uçtan uca doğrulama

Front-month uyarısı kategori sayfasına eklenir, uygulama gerçek veriyle tarayıcıda doğrulanır, Actions koşusu 23 seriyle yeşil görülür.

**Files:**
- Modify: `core/catalog.py` (`Kategori.note`)
- Modify: `core/page.py` (notu render et)
- Modify: `catalog/categories.yaml` (iki nota)
- Modify: `README.md` (yol haritası)
- Test: `tests/test_catalog.py`

**Interfaces:**
- Consumes: `core.catalog.Kategori`, `core.catalog.kategorileri_yukle`
- Produces: `core.catalog.Kategori(slug, title, note: str | None = None)`

- [ ] **Step 1: Test yaz (başarısız olmalı)**

`tests/test_catalog.py`'ye ekleyin:

```python
def test_kategori_notu_okunur():
    kategoriler = {k.slug: k for k in kategorileri_yukle()}
    assert kategoriler["emtia-enerji"].note is not None
    assert "front-month" in kategoriler["emtia-enerji"].note
    assert kategoriler["enflasyon"].note is None
```

- [ ] **Step 2: Testi koştur, başarısız olduğunu gör**

Run: `.venv/bin/python -m pytest tests/test_catalog.py::test_kategori_notu_okunur -v`
Expected: FAIL — `AttributeError: 'Kategori' object has no attribute 'note'`

- [ ] **Step 3: `Kategori`'ye `note` ekle**

`core/catalog.py` içinde:

```python
@dataclass(frozen=True)
class Kategori:
    slug: str
    title: str
    note: str | None = None
```

ve `kategorileri_yukle` içindeki kurucuyu:

```python
        kategoriler.append(
            Kategori(slug=slug, title=ham["title"], note=ham.get("note"))
        )
```

- [ ] **Step 4: Notları katalogda tanımla**

`catalog/categories.yaml`'daki iki emtia kaydını şununla değiştirin:

```yaml
- slug: emtia-enerji
  title: Emtia · Enerji
  note: >-
    Fiyatlar sürekli ön vade (front-month) kontratlarıdır; vade
    değişimlerinde grafikte görülen sıçrama piyasa hareketi değil
    kontrat roll'üdür.
- slug: emtia-metaller
  title: Emtia · Metaller
  note: >-
    Fiyatlar sürekli ön vade (front-month) kontratlarıdır; vade
    değişimlerinde grafikte görülen sıçrama piyasa hareketi değil
    kontrat roll'üdür.
```

- [ ] **Step 5: Notu sayfada göster**

`core/page.py` içinde `_kategoriyi_ciz` fonksiyonundaki şu satırın hemen altına:

```python
    st.caption(f"{len(seriler)} seri · Kaynak: {', '.join(kaynaklar)}")
```

ekleyin:

```python
    if kategori.note:
        st.caption(kategori.note)
```

Bu, Faz 2'nin sayfa katmanına yaptığı tek dokunuştur.

- [ ] **Step 6: Testleri koştur**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS — 85 test.

- [ ] **Step 7: Uygulamayı tarayıcıda doğrula**

```bash
.venv/bin/streamlit run app.py
```

Doğrulayın:

1. Sol menüde altı kategori: Genel Bakış, Ekonomi & Makro, Emtia · Enerji, Emtia · Metaller, Enflasyon Verileri, İnşaat, Kredi Kartı Harcamaları
2. `/emtia-enerji` ve `/emtia-metaller` doğrudan açılıyor ("Page not found" yok)
3. Her iki sayfada front-month uyarısı başlığın altında görünüyor
4. KPI satırı gerçek fiyatlar gösteriyor; Metaller'de Altın/Gümüş/Bakır/HRC Çelik
5. Seviye grafikleri 15 yıllık geçmişi çiziyor; mevsimsellik grafikleri Oca→Ara ekseninde üç yıl
6. `YoY %` görünümüne geçince grafikler yüzdeye dönüyor, y ekseni `%` oluyor
7. Tarayıcı konsolunda hata yok

Ekran görüntüsü alın.

- [ ] **Step 8: README yol haritasını güncelle**

`README.md`'deki yol haritası tablosunda Faz 2 satırını şununla değiştirin:

```
| 2 | Emtia (10 seri, 2 kategori): Brent, WTI, doğalgaz, altın, gümüş, bakır, HRC çelik, platin, paladyum, alüminyum | ✅ |
```

Ayrıca "Yeni seri ekleme" bölümündeki YAML örneğine `kaynak_tipi: evds` satırını ekleyin (artık zorunlu alan) ve altına bir cümle:

```
Yahoo Finance serisi için `kaynak_tipi: yahoo` ve `evds_code`/`evds_frequency`
yerine `yahoo_symbol: "BZ=F"` kullanın.
```

- [ ] **Step 9: Commit ve push**

```bash
git add core/catalog.py core/page.py catalog/categories.yaml README.md tests/test_catalog.py
git commit -m "feat: kategori notu ve front-month uyarısı

Kategori opsiyonel bir note alanı kazandı; emtia sayfalarında sürekli
ön vade uyarısı gösteriliyor."
git push -u origin feat/faz2-emtia
```

- [ ] **Step 10: Actions'ı 23 seriyle doğrula**

```bash
gh workflow run "Veri güncelle" --ref feat/faz2-emtia
gh run watch
```

Expected: yeşil, `23/23 seri başarılı`. Veri Task 3'te çekildiği için "Veride değişiklik yok" demesi normaldir.

---

## Self-Review

**Spec coverage:**

| Spec gereksinimi | Karşılayan görev |
|---|---|
| Katalog şeması `kaynak_tipi` ile ayrışır | Task 1 |
| `_dogrula` kaynak tipine göre doğrular | Task 1 |
| 13 mevcut seri `kaynak_tipi: evds` alır | Task 1 |
| Yahoo kontratı (endpoint, UA, sembol kodlama, tarih yuvarlama, null filtreleme) | Task 2 |
| DST sınırında tarih yuvarlama testi | Task 2 |
| Hız sınırlama eklenmez | Task 2 (yok) |
| Yedek kaynak eklenmez | Task 2 (yok, docstring'de belgeli) |
| `run.py` kaynak tipine göre açık dallanır | Task 3 |
| `EVDS_API_KEY` kontrolü koşullu | Task 3 |
| İki kategori, on seri, bilinçli sıra | Task 3 |
| Gerçek çekim, 23/23 | Task 3 |
| Front-month veri kalitesi uyarısı | Task 4 |
| Sayfa katmanı iddiası | Task 4 (tek satır, açıkça belirtildi) |
| Tarayıcı doğrulaması | Task 4 |
| Actions doğrulaması | Task 4 |
| CSV kalır, parquet yok | Global Constraints |

Boşluk yok.

**Kapsam dışı (spec'te de öyle):** Tarım (buğday, mısır — `USX` birim tuzağı), EPDK/BOTAŞ sayfaları, TÜİK tarım/hayvancılık sayfaları, Dünya Bankası Pink Sheet.

**Type consistency:** `Seri`'nin yeni alan sırası Task 1'de tanımlandı; Task 3'ün katalog girdileri bu şemaya uyuyor. `_cek(seri, api_key, oturum)` Task 3'te tanımlanıp aynı görevde test ediliyor. `yahoo.seri_cek(seri, session=None)` Task 2'de tanımlanıp Task 3'te aynı imzayla çağrılıyor — `bugun` parametresi yok, çünkü `range=15y` göreli. `Kategori.note` Task 4'te eklenip aynı görevde tüketiliyor.

**Bilinçli sapma:** Spec "sayfa katmanı değişmez" diyor; Task 4 `core/page.py`'ye kategori notunu render eden iki satır ekliyor. Bu, yeni verinin görünmesi için değil, veri kalitesi uyarısı için gereken katalog-güdümlü bir eklemedir. `components.py`, `charts.py`, `theme.py` ve `app.py` gerçekten hiç değişmiyor.
